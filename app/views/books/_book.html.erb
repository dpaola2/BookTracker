<div id="<%= dom_id book %>">
  <div class="row mt-3">
    <div class="col">
      <h3><%= book.title %></h3>
      <p>
        <strong>Author:</strong>
        <%= book.author %>
      </p>

      <p>
        <strong>Isbn:</strong>
        <%= book.isbn %>
      </p>

      <p>
        <strong>Shelf:</strong>
        <% if book.shelf.present? %>
          <%= book.shelf.name %>
        <% else %>
          <em>None</em>
        <% end %>
      </p>
    </div>
  </div>
</div>
