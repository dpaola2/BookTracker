<h1>Books</h1>

<div class="d-grid mb-3">
  <%= link_to "Search Books", book_searches_path, class: "btn btn-primary" %>
</div>

<div id="books" class="list-group mt-3">
  <% @books.each do |book| %>
    <%= render "books/list_item", book: book %>
  <% end %>
</div>


<%== pagy_bootstrap_nav(@pagy) if @pagy.pages > 1 %>