<h1>Shelves</h1>

<div id="shelves" class="list-group">
  <% @shelves.each do |shelf| %>
    <%= link_to shelf_path(shelf), class: "list-group-item list-group-item-action" do %>
      <h3><%= shelf.name %></h3>
      <%= shelf.books.count %> books
    <% end %>
  <% end %>
</div>

<%= link_to "New shelf", new_shelf_path %>
